import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'spring-boot-ionic-frontend',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
